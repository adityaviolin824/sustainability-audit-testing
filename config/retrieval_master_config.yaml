# config/retrieval_master_config.yaml

# --- Vectorstore Settings ---
vectorstore:
  collection_name: "brsr_audit_collection"
  embedding_model: "text-embedding-3-small" # small is enough
  db_path_relative: "chroma_db" 

# --- Retrieval Parameters ---
retrieval:
  initial_k: 25       
  final_k: 15       
  
# --- Intelligence Toggles ---
pipeline_logic:
  process_query: false   # Whether to run the Query Rewriting/Expansion step
  use_reranking: false   # Whether to run the LLM-based Re-ranking step

# --- Model Selection ---
models:
  query_expansion_model: "openai/gpt-5-nano"
  reranking_model: "openai/gpt-5-nano"

# --- Output Settings ---
output:
  demo_export_path: "testing/retrieval_demo.txt"
  encoding: "utf-8"